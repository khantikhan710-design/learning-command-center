# 多用户数据隔离：部署步骤

代码已改为由 `studyData` 云函数读取当前微信用户的 OpenID，并且只访问该用户自己的数据。

## 你需要在微信开发者工具完成一次部署

1. 在左侧文件树找到 `cloudfunctions/studyData`。
2. 右键文件夹，选择“上传并部署：云端安装依赖”。
3. 等待部署完成，再按 `Ctrl + R` 编译小程序。

## 收紧数据库权限

在云开发控制台中，分别打开这四个集合并将客户端权限设为“仅云函数可读写”或关闭客户端读写：

- `study_tasks`
- `study_records`
- `study_reviews`
- `study_focus_sessions`

小程序已不再从前端直接读写这些集合；云函数仍可正常读写。这样每个用户的数据由服务端按 OpenID 隔离。
