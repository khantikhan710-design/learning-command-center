# 刷新学习卡与备份状态设计

## 目标

让学习卡每次进入今日页都换一张，并让归档页明确显示本机完整备份的最近生成时间。

## 设计

- 学习卡使用本地轮换游标：每次今日页显示时取下一条，并写入本机存储；不联网，不重复请求。
- 完整备份复制成功后保存时间戳；归档页将其显示为“最近本机备份”。从未备份时显示明确提醒。
- 不伪造云端同步状态。云授权恢复前，应用仍是本机数据优先。

## 验收

1. 连续两次取学习卡得到相邻卡片，末尾可循环。
2. 有效备份时间显示可读文本；无时间显示未备份提醒。
3. 既有备份格式与恢复流程不变。
